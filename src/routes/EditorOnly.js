import React from 'react';

import EmailEditorOnlyView from '../Components/Email/EmailEditorOnly';

const EmailEditorOnly = () => (
  <div>
    <EmailEditorOnlyView />
  </div>
);

export default EmailEditorOnly;
